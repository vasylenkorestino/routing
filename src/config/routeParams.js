/** Default route parameters — flexible ranges, not hard limits. */
module.exports = {
  timeRange: { from: 120, to: 480 },
  distanceRange: { from: 20, to: 150 },
  stopsRange: { from: 5, to: 30 },
};
